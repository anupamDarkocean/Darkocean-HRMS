# Default: normalise line endings to LF in the repo
* text=auto eol=lf

# Shell scripts must always be LF — CRLF breaks the shebang in Linux containers
*.sh text eol=lf

# Windows-only files can keep CRLF
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
