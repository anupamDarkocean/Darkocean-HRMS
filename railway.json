{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "dockerfile"
  },
  "deploy": {
    "startCommand": "bench serve --port $PORT",
    "restartPolicyType": "on_failure",
    "restartPolicyMaxRetries": 5
  }
}
